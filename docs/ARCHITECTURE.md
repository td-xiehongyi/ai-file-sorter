# AI File Organizer 系统架构

| 属性 | 值 |
| --- | --- |
| 状态 | 阶段一已完成；阶段二规划中 |
| 目标版本 | `v0.1.0` |
| 最后更新 | `2026-08-18` |

## 文档目的

本文档说明系统边界、模块职责、依赖方向、关键数据流、持久化策略和测试分层。产品需求见[产品需求文档](./PRD.md)，文件扫描与操作的规范性规则见[安全模型](./SAFETY_MODEL.md)，分阶段建设内容见[开发路线图](./ROADMAP.md)。涉及安全边界的描述如有冲突，以安全模型为准。

## 架构目标

- 建立 React 前端与 Rust 后端之间明确、窄化的能力边界。
- 让扫描、搜索和基础浏览不依赖 AI Provider。
- 让用户操作与 AI 建议复用同一套校验、预览、确认和执行管线。
- 在本地完成格式识别与正文提取，使原始正文的生命周期与持久化结果明确分离。
- 以本地文件系统为真实状态来源，以 SQLite 保存可重建索引和应用持久数据。
- 不向前端或 AI 暴露 Shell、任意文件系统访问或未经计划绑定的执行能力。

## 系统边界

```text
┌──────────────── React / TypeScript ────────────────┐
│ 页面与组件 → feature 状态 → src/lib 受控调用       │
└──────────────────────┬─────────────────────────────┘
                       │ 结构化 Tauri 请求与响应
┌──────────────────────▼─────────────────────────────┐
│ Tauri Command → Rust service → models / storage    │
│                              ↘ 本地文件系统         │
└──────────────────────┬─────────────────────────────┘
                       │ 可选、经用户同意的数据请求
                 本地或远程 AI Provider
```

- React 负责展示、交互和提交结构化请求，不直接访问本地文件系统、SQLite 或 Shell。
- Tauri Command 是前后端唯一能力入口，只接收窄化请求并返回结构化结果。
- Rust service 负责路径策略、磁盘复核、内容提取、业务校验、安全执行和撤销资格判断。
- storage 负责系统应用数据路径、SQLite 迁移、文件索引、AI 分析结果和操作历史，但不保存提取出的原始正文。
- AI Provider 只生成建议，不连接文件执行器，也不持有执行计划的确认权限。

## 目录边界

```text
MyApp/
├── README.md
├── package.json
├── vite.config.ts
├── vitest.config.ts
├── tsconfig.json
├── docs/
│   ├── PRD.md
│   ├── ARCHITECTURE.md
│   ├── SAFETY_MODEL.md
│   └── ROADMAP.md
├── src/
│   ├── main.tsx
│   ├── app/
│   │   ├── App.tsx
│   │   ├── App.test.tsx
│   │   └── styles.css
│   └── test/
│       └── setup.ts
├── src-tauri/
│   ├── Cargo.toml
│   ├── tauri.conf.json
│   ├── capabilities/
│   │   └── default.json
│   ├── src/
│   │   ├── main.rs
│   │   ├── lib.rs
│   │   ├── commands/
│   │   ├── models/
│   │   ├── services/
│   │   └── storage/
│   └── tests/
│       └── app_smoke.rs
└── tests/e2e/                    # 阶段六规划，尚未创建
```

阶段一已建立可运行的前端入口、Tauri/Rust 装配边界和最小测试。`components/`、`features/`、`lib/`、`types/`、`src-tauri/src/ai/` 和 `tests/e2e/` 将在后续阶段按需创建，不预先放置空业务模块。

## 前端职责

### `src/app/`

- React 应用入口、根布局、全局样式和应用级组合逻辑。
- 可以组合业务 feature，但不承载具体文件管理规则。

### `src/components/`

- 存放无业务含义的共享 UI 组件，例如按钮、对话框、表格外壳和状态提示。
- 不直接调用 Tauri Command，也不包含扫描、文件操作或 AI 规则。

### `src/features/files/`

- 承载目录选择、扫描进度、文件列表、排序、筛选、搜索和变化提示。
- 只通过 `src/lib/` 中的受控接口请求后端能力。

### `src/features/operations/`

- 承载操作草案、From/To 预览、确认、执行结果、历史和撤销资格展示。
- 不能直接构造文件系统写入或绕过后端计划执行操作。

### `src/features/ai/`

- 阶段五加入，承载文件选择、内容分析状态、Provider 配置、按批数据发送同意、建议生成和逐项审查界面。
- 只产生或编辑操作草案，不持有直接文件执行能力。

### `src/lib/`

- 封装窄化的 Tauri 调用、请求响应映射和错误转换。
- 不提供接受任意命令名、路径操作或 Shell 命令的通用包装器。

### `src/types/`

- 描述文件条目、搜索条件、内容分析请求与结果、操作草案、预览、执行结果、历史和 AI 建议等共享类型。
- 类型与 Rust 对外数据保持一致，但不包含文件执行逻辑。

## Rust 后端职责

### `src-tauri/src/commands/`

- Tauri Command 的唯一入口目录。
- Command 只接收结构化请求、调用 service 并返回结构化结果，不实现复杂业务逻辑。

### `src-tauri/src/models/`

- 保存 Rust 领域模型和跨层数据结构。
- 未校验操作草案、已校验计划和执行结果使用不同结构表达，防止能力混淆。

### `src-tauri/src/services/`

- 承载扫描、搜索、监听、路径策略、格式识别、正文提取、分段与汇总、操作校验、安全执行和撤销资格判断。
- 文件执行器只接收后端已校验且仍有效的计划，不接收前端临时替换的路径。

### `src-tauri/src/storage/`

- 解析系统应用数据路径，管理 SQLite 连接、迁移、索引仓储、AI 分析结果仓储和操作历史仓储。
- 文件索引与操作历史使用不同的持久化职责，索引重建不能清除历史。

### `src-tauri/src/ai/`

- 阶段五加入，定义可替换 Provider 边界和模型调用适配。
- 本地与远程 Provider 共用同一结构化接口；远程适配器只能接收已绑定本批用户同意的数据请求。
- Provider 输出必须交给 suggestion service 解析为严格的结构化建议。
- AI 模块不能调用文件执行、历史清理或撤销服务。

## 依赖方向

```text
React feature
  → src/lib 中的受控 Tauri 调用
  → commands
  → services
  → models / storage / 本地文件系统
```

依赖只能沿箭头向下。共享 UI 不依赖业务 feature；models 不依赖 commands；storage 不接受前端指定的数据库路径；前端不能绕过 commands 访问 Rust service。

AI 功能加入后遵循：

```text
用户明确选择文件
  → Rust 识别真实格式并提取临时正文
  → 长文档分段；远程调用先绑定本批发送同意
  → AI Provider 生成结构化分析结果
  → suggestion service 校验摘要、关键词、文件名、分类 ID、置信度和理由
  → 分析结果持久化，原始正文销毁
  → 用户接受仍有效的建议并转换为携带内容指纹的 AI 派生草案
  → 操作校验与预览计划
  → 用户确认 planId
  → 安全执行
  → 历史记录与撤销资格判断
```

## 关键数据流

### 扫描与索引

```text
用户选择真实目录
  → Command 接收目录选择结果
  → path policy 建立授权根目录
  → scanner 只读遍历并汇总错误
  → repository 事务写入文件元数据
  → 前端展示进度与结果摘要
```

扫描器与监听器共享授权根目录和链接边界规则。单文件失败被汇总，不扩散为整个扫描任务崩溃。

### 搜索与变化同步

```text
前端提交结构化查询
  → search service 规范化搜索与排序条件
  → repository 分页读取索引
  → 前端渲染结果

磁盘变化事件
  → watcher 复核授权边界
  → 更新索引
  → 通知前端刷新
```

监听事件只更新索引，不创建操作草案，也不自动整理文件。

### 内容分析与 AI 建议

```text
用户选择文本型 PDF、TXT、MD 或 DOCX
  → content policy 复核授权范围、文件身份与真实格式
  → extractor 在本地提取正文到任务内存并同步计算内容指纹
  → chunker 根据 Provider 输入预算分段
  → 短文档直接分析；长文档逐段摘要后汇总
  → suggestion service 校验结构化输出和现有分类 ID
  → repository 保存分析结果与文件身份，不保存原始正文
  → 前端展示摘要、关键词、建议、置信度、理由和采用状态
```

目录扫描和监听不会触发正文提取。远程 Provider 调用前，consent policy 生成一次性批次同意凭据，绑定确切文件版本、Provider/模型、拟发送字段、预计字符或 token 数、分段与调用次数、最大重试预算和费用范围；Provider 无法估算费用时明确显示“无法估算”。任一绑定项变化、超出预算、取消、消费或过期都会使凭据失效，远程适配器只有持有有效凭据才能发送正文。本地 Provider 不发生数据外发，但仍展示本次处理范围。

Provider 结果使用封闭 schema：`summary: string`、`keywords: string[]`、`suggested_filename: string`、`category_id: string | null`、`confidence: number`、`reason: string`。字符串必填项不得为空，文件名不得包含路径且扩展名必须与源文件一致，置信度必须位于 `[0, 1]`，不接受额外字段。分类建议使用用户配置的分类 ID；Rust 将有效分类 ID 映射到已存在目标目录。AI 输出的绝对路径、未知分类或目录创建请求一律不能转换为操作草案；`category_id` 为 `null` 时保留原目录，只允许重命名建议。

内容提取在读取文件字节时同步计算专用于 AI 结果有效性的内容指纹，并在提取结束及持久化前复核元数据与平台身份；分析期间发生变化时丢弃结果。持久结果绑定该指纹，应用重启后加载或转换为操作草案前按需重新计算并核对。AI 派生的草案和已校验计划继续携带内容指纹，执行器在执行前再次复核；不一致时结果或计划失效。该指纹只进入 AI 派生操作，手动普通操作计划仍不强制计算内容哈希。扫描 PDF、加密或损坏文档、空文本、格式伪装、不支持格式、解码失败和资源限制等错误使用结构化状态返回，不调用 Provider 或生成可执行建议。

### 安全文件操作

```text
用户输入或 AI 建议
  → 未信任操作草案
  → Rust 整批校验
  → 内存中的一次性计划与 From/To 预览
  → 用户只提交 planId 确认
  → Rust 执行前复核文件状态；AI 派生计划额外复核内容指纹
  → 逐项执行并记录真实结果
  → 持久历史与动态撤销资格
```

计划生命周期、文件身份、冲突、批量失败和撤销的具体行为由[安全模型](./SAFETY_MODEL.md)定义。

## 持久化边界

- 本地文件系统是文件存在、身份和内容状态的最终事实来源。
- SQLite 统一位于操作系统提供的应用数据目录，不在扫描根目录写入数据库或旁车文件。
- 文件索引保存可重建元数据，可通过重新扫描恢复。
- AI 分析结果保存摘要、关键词、建议、置信度、理由、Provider/模型标识、分析时间、文件身份、内容指纹和采用状态；提取出的原始正文只存在于当前任务内存中，不写入 SQLite、历史或日志。
- 操作历史保存实际操作和逐项结果，不随索引重建清除。
- 短期 `planId` 只存在于 Rust 进程内存中，不写入 SQLite；应用退出后失效。
- 前端不能指定数据库位置、直接访问 SQLite 或提交任意 SQL。

## 错误与状态表达

- Command 返回结构化成功或错误结果，前端负责将其转换为用户可理解的状态提示。
- 扫描类任务汇总可恢复的单项错误，并保留已获得的有效结果。
- 校验类错误阻止生成可确认计划，不产生文件变化或成功历史。
- 批量执行使用成功、失败、未执行三种逐项状态表达真实磁盘结果。
- Provider 错误只影响 AI 建议流程，不使扫描、搜索或手动文件操作不可用。
- 内容提取失败、不支持格式、正文为空、用户拒绝远程发送或结果过期都返回独立状态，不生成操作草案。

## 测试分层

- 前端组件和 hook 单元测试与对应 feature 放在一起，使用 `.test.ts` 或 `.test.tsx` 后缀。
- Rust 纯逻辑单元测试放在对应模块内部。
- 涉及 SQLite、临时文件系统或多个 Rust 模块的测试放入 `src-tauri/tests/`。
- 内容分析集成测试使用人工生成的非敏感 PDF、TXT、MD 和 DOCX 样本以及模拟 Provider，并验证严格 schema、有效分类映射、一次性批次同意、原始正文不进入数据库/历史/日志，以及同大小或相同修改时间替换、分析期间变化、重启后加载和 AI 草案生成后至执行前的内容指纹复核。
- 跨前后端桌面流程和平台差异测试放入 `tests/e2e/`。
- 所有文件系统和 SQLite 测试使用隔离临时目录与独立测试数据库，不接触用户真实数据。
- 阶段一至五随功能同步建设测试；阶段六补充跨平台、恢复、性能和发布验证，不替代底层测试。

## 当前状态

- 阶段一已完成：仓库可运行 Tauri 2、React、TypeScript、Rust 和 Tailwind CSS 最小应用。
- 前端仅展示 Local First 定位、建设状态和安全原则，不调用 Tauri API。
- Rust 已建立 `commands`、`models`、`services` 和 `storage` 模块边界，但没有自定义 Command 或业务实现。
- SQLite、目录扫描、文件操作、AI Provider 与相关权限均尚未引入；后续改动必须按[开发路线图](./ROADMAP.md)逐阶段确认。
