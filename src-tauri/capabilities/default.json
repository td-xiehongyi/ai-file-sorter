{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimal capability for the main application window",
  "windows": ["main"],
  "permissions": ["dialog:default"]
}
